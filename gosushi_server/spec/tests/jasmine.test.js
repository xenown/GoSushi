describe('name of function', function() {
    it('should be formatted like a sentence (aka starts with "should") and describe the output', function() {
        var summation = 1 + 2;
        expect(summation).toBe(3);
    })
})